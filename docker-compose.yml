services:
  gitea:
    image: gitea/gitea:latest
    restart: always
    volumes:
      - git_data:/data
    ports:
      - 3232:3000
  db:
    image: postgres:alpine
    restart: always
    volumes:
      - db_data:/var/lib/postgresql/data
    expose:
      - 5432
volumes:
  db_data:
  git_data:
